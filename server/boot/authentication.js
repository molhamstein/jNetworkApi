'use strict';

module.exports = function enableAuthentication(server) {
  // enable authentication
//   server.enableAuth();

//   server.models.partner.find({where : {email : 'a@a.com'}},function(err,user){
//   	// console.log(user)

//   	server.models.Role.find({where : {name : 'admin'}},function(err,admin){
//   		// console.log(admin)
//   		// admin[0].principals.create({
// 	   //      principalType: 'partner',
// 	   //      principalId: user[0].id
// 	   //    }, function(err, principal) {
// 	   //      if (err) throw err;

// 	   //      console.log('Created principal:', principal);
// 	   //    });
//   	});
//   })
};
